<%= fetch(:rails_env) %>:
  adapter: postgresql
  timeout: 5000
  encoding: utf8
  database: <%= "#{fetch(:short_name)}_#{fetch(:rails_env)}" %>
  pool: 5
  host: localhost
  username: <%= "#{fetch(:application)}_role" %>
  password: <%= fetch(:db_passwd) %>
